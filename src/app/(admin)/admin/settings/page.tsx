import { RoutePlaceholder } from "@/components/placeholders/route-placeholder";

export default function AdminSettingsPage() {
  return <RoutePlaceholder eyebrow="Admin settings" title="AI and app settings" description="Route placeholder for provider status, knowledge-base IDs, sync state, and app configuration." />;
}
